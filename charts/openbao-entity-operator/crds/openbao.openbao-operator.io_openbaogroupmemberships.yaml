---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: openbaogroupmemberships.openbao.openbao-operator.io
spec:
  group: openbao.openbao-operator.io
  names:
    categories:
    - openbao
    kind: OpenBaoGroupMembership
    listKind: OpenBaoGroupMembershipList
    plural: openbaogroupmemberships
    singular: openbaogroupmembership
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.groupID
      name: Group ID
      type: string
    - jsonPath: .status.memberID
      name: Member ID
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenBaoGroupMembership is the Schema for the openbaogroupmemberships
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenBaoGroupMembershipSpec defines one explicit membership
              edge.
            properties:
              entityRef:
                description: EntityRef selects an OpenBaoEntity to add to the parent
                  group.
                properties:
                  name:
                    description: Name is the OpenBaoEntity resource name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              groupRef:
                description: GroupRef selects the parent OpenBaoGroup in the same
                  namespace.
                properties:
                  name:
                    description: Name is the OpenBaoGroup resource name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              memberGroupRef:
                description: MemberGroupRef selects an OpenBaoGroup to add as a subgroup
                  of the parent group.
                properties:
                  name:
                    description: Name is the OpenBaoGroup resource name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - groupRef
            type: object
            x-kubernetes-validations:
            - message: groupRef is immutable; delete and recreate the OpenBaoGroupMembership
              rule: self.groupRef == oldSelf.groupRef
            - message: entityRef is immutable; delete and recreate the OpenBaoGroupMembership
              rule: '!has(oldSelf.entityRef) || (has(self.entityRef) && self.entityRef
                == oldSelf.entityRef)'
            - message: memberGroupRef is immutable; delete and recreate the OpenBaoGroupMembership
              rule: '!has(oldSelf.memberGroupRef) || (has(self.memberGroupRef) &&
                self.memberGroupRef == oldSelf.memberGroupRef)'
            - message: exactly one of entityRef or memberGroupRef must be set
              rule: has(self.entityRef) != has(self.memberGroupRef)
          status:
            description: OpenBaoGroupMembershipStatus defines the observed state of
              one membership edge.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the membership.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              groupID:
                description: GroupID is the stable parent OpenBao group identifier.
                type: string
              memberID:
                description: MemberID is the stable entity or subgroup identifier.
                type: string
              memberType:
                description: MemberType identifies whether MemberID is an entity or
                  subgroup.
                enum:
                - Entity
                - Group
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation reflected
                  in status.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
