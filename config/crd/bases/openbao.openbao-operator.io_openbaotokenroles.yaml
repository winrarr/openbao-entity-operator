---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: openbaotokenroles.openbao.openbao-operator.io
spec:
  group: openbao.openbao-operator.io
  names:
    categories:
    - openbao
    kind: OpenBaoTokenRole
    listKind: OpenBaoTokenRoleList
    plural: openbaotokenroles
    singular: openbaotokenrole
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.configHash
      name: Config Hash
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenBaoTokenRole is the Schema for the openbaotokenroles API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenBaoTokenRoleSpec defines a role in OpenBao's built-in
              token auth method.
            properties:
              allowedEntityAliases:
                description: AllowedEntityAliases limits entity aliases that may be
                  attached to tokens from this role.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              allowedPolicies:
                description: AllowedPolicies limits policies that callers may request
                  for generated tokens.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              allowedPoliciesGlob:
                description: AllowedPoliciesGlob limits policies using OpenBao glob
                  patterns.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              connectionRef:
                description: ConnectionRef selects the OpenBao API connection in the
                  same namespace.
                properties:
                  name:
                    description: Name is the OpenBaoConnection resource name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              creationPolicy:
                default: Create
                description: CreationPolicy controls how an external role is acquired.
                enum:
                - Create
                - Adopt
                - CreateOrAdopt
                type: string
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether the external role is
                  deleted with this resource.
                enum:
                - Delete
                - Orphan
                type: string
              disallowedPolicies:
                description: DisallowedPolicies rejects requested policies in this
                  set.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              disallowedPoliciesGlob:
                description: DisallowedPoliciesGlob rejects requested policies matching
                  these globs.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              driftDetectionInterval:
                description: |-
                  DriftDetectionInterval controls periodic checks for changes made outside Kubernetes.
                  A zero duration disables periodic checks. When omitted, the operator default is used.
                type: string
              orphan:
                description: Orphan controls whether generated tokens have no parent
                  token.
                type: boolean
              pathSuffix:
                description: PathSuffix adds a suffix to generated token paths for
                  targeted revocation.
                type: string
              renewable:
                description: Renewable controls whether generated tokens can be renewed.
                type: boolean
              tokenBoundCIDRs:
                description: TokenBoundCIDRs restricts issued tokens to these client
                  address ranges.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              tokenExplicitMaxTTL:
                description: TokenExplicitMaxTTL gives issued tokens an explicit maximum
                  TTL.
                type: string
              tokenNoDefaultPolicy:
                description: TokenNoDefaultPolicy prevents OpenBao from adding the
                  default policy.
                type: boolean
              tokenNumUses:
                description: TokenNumUses limits how many times an issued token may
                  be used. Zero means unlimited.
                format: int64
                type: integer
              tokenPeriod:
                description: TokenPeriod gives issued tokens a fixed renewable period.
                type: string
              tokenType:
                description: TokenType selects the type of token generated by the
                  role.
                enum:
                - service
                - batch
                type: string
            required:
            - connectionRef
            type: object
            x-kubernetes-validations:
            - message: connectionRef is immutable; delete and recreate the OpenBaoTokenRole
              rule: self.connectionRef == oldSelf.connectionRef
          status:
            description: OpenBaoTokenRoleStatus defines the observed state of an OpenBao
              token role.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the role.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: ConfigHash is the SHA-256 hash of the normalized role
                  configuration.
                type: string
              name:
                description: Name is the OpenBao role name.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation reflected
                  in status.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
