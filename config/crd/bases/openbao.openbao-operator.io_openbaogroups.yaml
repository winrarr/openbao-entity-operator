---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: openbaogroups.openbao.openbao-operator.io
spec:
  group: openbao.openbao-operator.io
  names:
    categories:
    - openbao
    kind: OpenBaoGroup
    listKind: OpenBaoGroupList
    plural: openbaogroups
    singular: openbaogroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.id
      name: OpenBao ID
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenBaoGroup is the Schema for the openbaogroups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenBaoGroupSpec defines the desired state of an OpenBao
              identity group.
            properties:
              connectionRef:
                description: ConnectionRef selects the OpenBao API connection in the
                  same namespace.
                properties:
                  name:
                    description: Name is the OpenBaoConnection resource name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              creationPolicy:
                default: Create
                description: CreationPolicy controls how an external group is acquired.
                enum:
                - Create
                - Adopt
                - CreateOrAdopt
                type: string
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether the external group is
                  deleted with this resource.
                enum:
                - Delete
                - Orphan
                type: string
              driftDetectionInterval:
                description: |-
                  DriftDetectionInterval controls periodic checks for changes made outside Kubernetes.
                  A zero duration disables periodic checks. When omitted, the operator default is used.
                type: string
              metadata:
                additionalProperties:
                  type: string
                description: Metadata is the desired key-value metadata on the OpenBao
                  group.
                type: object
              policies:
                description: Policies is the desired set of OpenBao ACL policy names.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              type:
                default: Internal
                description: |-
                  Type selects an internal or external OpenBao group. Membership resources
                  are supported for internal groups.
                enum:
                - Internal
                - External
                type: string
            required:
            - connectionRef
            type: object
            x-kubernetes-validations:
            - message: connectionRef is immutable; delete and recreate the OpenBaoGroup
              rule: self.connectionRef == oldSelf.connectionRef
          status:
            description: OpenBaoGroupStatus defines the observed state of OpenBaoGroup.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the group.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              id:
                description: ID is the stable OpenBao group identifier.
                type: string
              managedMemberEntityIDs:
                description: |-
                  ManagedMemberEntityIDs tracks entity memberships claimed by current or
                  previous OpenBaoGroupMembership resources so deletion removes only owned edges.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              managedMemberGroupIDs:
                description: |-
                  ManagedMemberGroupIDs tracks subgroup memberships claimed by current or
                  previous OpenBaoGroupMembership resources so deletion removes only owned edges.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              memberEntityIDs:
                description: MemberEntityIDs contains all entity IDs currently assigned
                  to the group.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              memberGroupIDs:
                description: MemberGroupIDs contains all subgroup IDs currently assigned
                  to the group.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              metadata:
                additionalProperties:
                  type: string
                description: Metadata is the metadata observed on the OpenBao group.
                type: object
              name:
                description: Name is the name returned by OpenBao.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation reflected
                  in status.
                format: int64
                type: integer
              policies:
                description: Policies is the policy set observed on the OpenBao group.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              type:
                description: Type is the observed OpenBao group type.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
