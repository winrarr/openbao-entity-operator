# This rule is not used by the project openbao-entity-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over openbao.openbao-operator.io.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: openbao-entity-operator
    app.kubernetes.io/managed-by: kustomize
  name: openbao-openbaoconnection-admin-role
rules:
- apiGroups:
  - openbao.openbao-operator.io
  resources:
  - openbaoconnections
  verbs:
  - '*'
- apiGroups:
  - openbao.openbao-operator.io
  resources:
  - openbaoconnections/status
  verbs:
  - get
