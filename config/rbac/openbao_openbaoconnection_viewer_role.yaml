# This rule is not used by the project openbao-entity-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to openbao.openbao-operator.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: openbao-entity-operator
    app.kubernetes.io/managed-by: kustomize
  name: openbao-openbaoconnection-viewer-role
rules:
- apiGroups:
  - openbao.openbao-operator.io
  resources:
  - openbaoconnections
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - openbao.openbao-operator.io
  resources:
  - openbaoconnections/status
  verbs:
  - get
